{
   "name":"scripts",
   "version":"1.0.0",
   "description":"Retrieve parameters from AWS Parameter Store and sets them as environment variables",
   "main":"ssm_get_secrets.js",
   "scripts":{
      "test":"echo \"Error: no test specified\" && exit 1"
   },
   "author":"nClouds DevOps",
   "license":"ISC",
   "dependencies":{
      "aws-sdk":"latest"
   }
}

